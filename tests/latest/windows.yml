---
- name: Test windows
  hosts: localhost
  tasks:
    - name: Check if node_exporter is running on port 9100  # noqa command-instead-of-module
      ansible.builtin.command: curl http://localhost:9100
      changed_when: false
